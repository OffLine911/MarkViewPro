# Build Directory

This directory contains build assets and compiled binaries.

## Contents

- `appicon.png` - Application icon (1024x1024 recommended)
- `bin/` - Compiled binaries (generated by `wails build`)

## Building

Run `wails build` from the project root to compile the application.

The output binary will be placed in the `bin/` subdirectory.
